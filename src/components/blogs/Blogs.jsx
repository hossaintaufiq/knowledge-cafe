import { useEffect, useState } from "react";

const Blogs = () => {

    const [blogs,setBlogs]=useState([]);

    useEffect(()=>{
        
    },[])

    return (
        <div>
            
        </div>
    );
};

export default Blogs;